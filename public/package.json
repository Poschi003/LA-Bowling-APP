{
  "name": "we2",
  "version": "0.1.0",
  "private": true,
  "description": "Private lokale Paar-App fuer Christian und Jenny.",
  "scripts": {
    "start": "node lan-server.js",
    "check": "node --check app.js && node --check lan-server.js && node --check server/we2-backend.js && node --check api/[route].js"
  },
  "dependencies": {
    "web-push": "^3.6.7"
  },
  "engines": {
    "node": ">=20"
  }
}
